interface:
  display_name: "Paid MCP Workflow"
  short_description: "Run search and bulk match-enrich credit workflows"
  default_prompt: "Use the paid or freemium MCP search and bulk match-enrich workflow with masked previews, aggregate insights, explicit record-count permission, server-side validation, and credit-aware responses."
